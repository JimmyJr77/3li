/**
 * Vercel entry: default-exported Express app (see Vercel "Express on Vercel").
 * Local development uses `server/src/index.ts` + Vite instead.
 */
export { default } from "../server/dist/app.js";

/**
 * Debounced autosave: persist this many milliseconds after the last change.
 * Used across editors and settings so behavior stays consistent app-wide.
 */
export const AUTOSAVE_DEBOUNCE_MS = 2000;
